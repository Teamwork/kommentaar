swagger: "2.0"
info:
  title: x
  version: x
consumes:
  - application/json
produces:
  - application/json
paths:
  /path:
    get:
      operationId: GET_path
      produces:
        - application/json
      responses:
        200:
          description: 200 OK
          schema:
            $ref: '#/definitions/wrapped-resp.bikeResp'
definitions:
  wrapped-resp.bikeResp:
    title: bikeResp
    type: object
    properties:
      bike:
        title: bikeResp
        type: object
        properties:
          Colour:
            type: string
