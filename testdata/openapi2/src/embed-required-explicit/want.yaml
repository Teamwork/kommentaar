swagger: "2.0"
info:
  title: x
  version: x
consumes:
  - application/json
produces:
  - application/json
paths:
  /path:
    post:
      operationId: POST_path
      produces:
        - application/json
      responses:
        200:
          description: 200 OK
          schema:
            $ref: '#/definitions/embed-required-explicit.resp'
definitions:
  embed-required-explicit.resp:
    title: resp
    description: resp docs.
    type: object
    required:
      - name
      - kind
    properties:
      id:
        type: integer
      kind:
        type: string
      name:
        type: string
      own:
        type: string
