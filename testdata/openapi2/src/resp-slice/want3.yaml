openapi: 3.0.3
info:
  title: x
  version: x
paths:
  /path:
    post:
      operationId: POST_path
      responses:
        "200":
          description: 200 OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/resp-slice.resp'
components:
  schemas:
    resp-slice.resp:
      type: array
      items:
        title: resp
        description: resp docs.
        type: object
        properties:
          foo:
            type: string
