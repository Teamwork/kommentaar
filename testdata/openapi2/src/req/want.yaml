swagger: "2.0"
info:
  title: x
  version: x
consumes:
- application/json
produces:
- application/json
paths:
  /path:
    post:
      operationId: POST_path
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - name: req.reqRef
        in: body
        required: true
        schema:
          $ref: '#/definitions/req.reqRef'
      responses:
        200:
          description: 200 OK
definitions:
  req.reqRef:
    title: reqRef
    type: object
