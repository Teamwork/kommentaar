openapi: 3.0.3
info:
  title: x
  version: x
paths:
  /path:
    post:
      operationId: POST_path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/req.reqRef'
      responses:
        "200":
          description: 200 OK (no data)
components:
  schemas:
    req.reqRef:
      title: reqRef
      type: object
