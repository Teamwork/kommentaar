swagger: "2.0"
info:
  title: x
  version: x
consumes:
- application/json
produces:
- application/json
paths:
  /path:
    post:
      operationId: POST_path
      produces:
      - application/json
      responses:
        200:
          description: 200 OK
          schema:
            $ref: '#/definitions/default-values.ref'
definitions:
  default-values.ref:
    title: ref
    type: object
    properties:
      field:
        type: string
        default: field value
      id:
        type: integer
        default: 42
      str:
        type: string
        default: "666"
