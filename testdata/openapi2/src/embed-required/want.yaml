swagger: "2.0"
info:
  title: x
  version: x
consumes:
  - application/json
produces:
  - application/json
paths:
  /path:
    post:
      operationId: POST_path
      produces:
        - application/json
      responses:
        200:
          description: 200 OK
          schema:
            $ref: '#/definitions/embed-required.resp'
definitions:
  embed-required.resp:
    title: resp
    description: resp docs.
    type: object
    required:
      - own
      - id
      - name
      - kind
    properties:
      code:
        type: string
      dup:
        type: string
      id:
        type: integer
      kind:
        type: string
      name:
        type: string
      note:
        type: string
      own:
        type: string
      shadowed:
        type: string
