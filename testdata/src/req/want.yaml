openapi: 3.0.1
paths:
  /path:
    post:
      operationId: POST_path
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/req.reqRef'
      responses:
        200:
          description: 200 OK
          content: {}
components:
  schemas:
    req.reqRef:
      title: reqRef

