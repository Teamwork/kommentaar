openapi: 3.0.1
paths:
  /path/{id}:
    post:
      operationId: POST_path_{id}
      tags:
      - tag
      parameters:
      - $ref: '#/components/parameters/path.pathRef'
      responses:
        200:
          description: 200 OK
          content: {}
components:
  schemas: {}
  parameters:
    path.pathRef:
      name: pathRef
      in: path
      schema:
        title: pathRef
