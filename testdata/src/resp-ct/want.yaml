openapi: 3.0.1
paths:
  /path:
    post:
      operationId: POST_path
      responses:
        200:
          description: 200 OK
          content:
            text/plain:
              schema:
                $ref: '#/components/schemas/req.respRef'
components:
  schemas:
    req.respRef:
      title: respRef

